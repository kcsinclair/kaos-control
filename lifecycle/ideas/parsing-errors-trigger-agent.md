---
title: Parsing Errors can Trigger an Agent to resolve
type: idea
status: draft
lineage: parsing-errors-trigger-agent
priority: normal
labels:
    - artefacts
    - workflow
    - usability
---

# Parsing Errors can Trigger an Agent to resolve

When a parsing error is identified, you can press a button to recommend a fix, which triggers an agent to review and recommend a fix, e.g. adding missing frontmatter to a new idea.
