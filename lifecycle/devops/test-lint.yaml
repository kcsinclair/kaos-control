name: Run Lint Tests
type: test

steps:
  - name: Lint
    description: >-
      make lint — the single gate for backend (go vet, staticcheck,
      govulncheck, gosec, gitleaks) and frontend (ESLint, vue-tsc) checks.
    command: make lint
    timeout: 4m
